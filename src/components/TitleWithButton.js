import styled from "styled-components";

const TitleWidthButton = styled.div`
	display: flex;
	justify-content: space-between;
	align-items: center;

	.button {
		margin-top: 0;
		margin-bottom: 0;
	}
`;

export default TitleWidthButton;
